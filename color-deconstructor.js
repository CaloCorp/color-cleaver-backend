const colorDeconstructor = function (color){
    let secondaryColors = ['orange','purple','green']

if (secondaryColors.includes(color) ) 

return 'a string'

else return "Sorry, that's not a secondary color!"

}

module.exports = colorDeconstructor